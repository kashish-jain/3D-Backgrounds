// //This file is for raycasting and adding interactivity to the objects

// import THREE from "../dist/three"

// function addInteractivity(raycaster, mouse, camera, scene) {
//     raycaster.setFromCamera( mouse, camera );
// 	let intersects = raycaster.intersectObjects( scene.children );
// 	intersects[0].point.set(10,10,10);
// 	// for ( let i = 0; i < intersects.length; i++ ) {
// 	// 	intersects[i].object.material.color.set( 0xff0000 );
// 	// }
// }

// export {addInteractivity};